pipeline {

        agent any
        stages{

                        stage(clean){
                        steps{
                            dir('Ajay-MBPline'){
                                    sh 'mvn clean'
                            }
                        }
        }
                stage(install){
                        steps{
                            dir('Ajay-MBPline'){
                                    sh 'mvn install'
                        }
        }
                            post{
                                always{
                                    archiveArtifacts artifacts: 'target/*.jar', followSymlinks: false
                                    junit 'target/surefire-reports/*.xml'
                                }
                            }
                       }
                                stage('clean ws'){
                                steps{
                                clean Ws()
                        }
                     }
                 }
             }
